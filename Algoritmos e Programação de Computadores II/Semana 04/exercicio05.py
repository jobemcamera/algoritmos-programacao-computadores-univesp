# Listas
# Lista Sequencial x Encadeada (ponteiros)

l = [1, 2, 13, 1213, 53]
for i in range(len(l)):
    print(l[i]) # podemos acessar os elementos diretamente pelos índices

# Por padrão, em Python é utilizado lista sequencial de abordagem dinâmica
